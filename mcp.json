{
  "mcpServers": {
    "lsp": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-lsp"],
      "description": "Production LSP Code Intelligence Server providing real-time diagnostics, definition jumps, and hover documentation"
    },
    "terminal": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-terminal"],
      "description": "Production PTY Terminal Supervisor managing background daemons, watchers, and non-blocking I/O streams"
    },
    "puppeteer": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-puppeteer"],
      "description": "Headless Browser Automation Engine for full-page screenshots, DOM extraction, and visual verification"
    },
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_TOKEN}"
      },
      "description": "GitHub Forge CI/CD integration for PR reviews, issue triage, and workflow run telemetry"
    },
    "semgrep": {
      "command": "uvx",
      "args": ["semgrep-mcp"],
      "description": "Static Application Security Testing (SAST) and high-entropy secret scanner"
    },
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "."],
      "description": "Local workspace filesystem provider with sandboxed read/write capabilities"
    },
    "git": {
      "command": "uvx",
      "args": ["mcp-server-git", "--repository", "."],
      "description": "Local Git repository introspection, branch log, and diff provider"
    },
    "sqlite": {
      "command": "uvx",
      "args": ["mcp-server-sqlite", "--db-path", "test.db"],
      "description": "Embedded relational database query and schema provider"
    },
    "fetch": {
      "command": "uvx",
      "args": ["mcp-server-fetch"],
      "description": "Resilient HTTP/HTTPS web resource and API content fetcher"
    }
  }
}
