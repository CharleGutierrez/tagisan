# Convex Schema

Review:

1. schema completeness and naming
2. optional vs nullable consistency
3. index/query alignment
4. search index and filter coverage where relevant

Prefer one canonical schema path; avoid duplicated or parallel table contracts.

