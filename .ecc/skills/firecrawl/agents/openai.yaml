interface:
  display_name: "Firecrawl"
  short_description: "Use Firecrawl CLI for cache-aware web extraction"
  default_prompt: "Use $firecrawl to reuse local .firecrawl artifacts, then run Firecrawl CLI commands: search, scrape, map, crawl, agent, interact, parse, monitor, research, feedback, doctor, or x download."
policy:
  allow_implicit_invocation: true
