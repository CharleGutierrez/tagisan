interface:
  display_name: "Subspawn"
  short_description: "Bounded subagent delegation with strict wait synthesis"
  default_prompt: "Use $subspawn to plan bounded subagent delegation, wait for all spawned agents, and synthesize evidence."
policy:
  allow_implicit_invocation: true
