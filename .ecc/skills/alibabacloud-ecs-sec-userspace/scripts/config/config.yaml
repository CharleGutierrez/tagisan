# sec-userspace client configuration
# Generated by packager
# DO NOT EDIT MANUALLY

client:
  instance_id: "${INSTANCE_UUID}"
  version: "1.4.1"

  server:
    endpoint: ""
    port: 8080
    protocol: "http"
    tls_verify: false

  upload:
    policy: "full"
    anonymize: false
    improvement_program:
      enabled: false
      require_consent: false

  privacy:
    hash_algorithm: "sha256"
    hmac_secret_env: "SEC_HMAC_SECRET"
