interface:
  display_name: "Ai Sdk UI"
  short_description: "Help with Ai Sdk UI tasks"
  default_prompt: "Use $ai-sdk-ui to build or debug AI SDK React chat UIs, tool UIs, streaming state, message persistence, and generative UI."
policy:
  allow_implicit_invocation: false
