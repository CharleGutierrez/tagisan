# Query ESLint

Use TanStack Query ESLint rules where the repo supports ESLint.

## Rules

- Prefer the official plugin for stable query clients, exhaustive deps, no unstable deps, and query option preference.
- In Biome-only repos, keep the guidance as review criteria unless ESLint is intentionally added.
- Do not add ESLint only for Query rules without checking repo tool policy.
