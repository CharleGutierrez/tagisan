{
  "evals": [
    {
      "id": "typegpu-task-1",
      "prompt": "In src/components/Hero.ts, implement TypeGPU behavior for a product surface and explain the validation gates.",
      "expected_output": "Uses typegpu routing, checks installed versions, applies domain-specific WebGPU guidance, runs or recommends the bundled audit CLI where relevant, and reports validation evidence.",
      "assertions": [
        "Mentions the relevant installed package/runtime/version checks for WebGPU.",
        "Uses at least one typegpu bundled reference or explains why no extra reference was needed.",
        "Includes reduced-motion/accessibility or explains why the effect is essential and already covered.",
        "Includes cleanup/interruption/runtime validation appropriate to TypeGPU schemas, typed buffers/textures, shader functions, pipelines, WebGPU capability checks, and CPU/GPU resource ownership.",
        "Does not route the task to an unrelated near-miss skill."
      ]
    },
    {
      "id": "typegpu-task-2",
      "prompt": "Review this branch for WebGPU motion issues: package versions look current but users report jank and missing reduced-motion behavior.",
      "expected_output": "Uses typegpu routing, checks installed versions, applies domain-specific WebGPU guidance, runs or recommends the bundled audit CLI where relevant, and reports validation evidence.",
      "assertions": [
        "Mentions the relevant installed package/runtime/version checks for WebGPU.",
        "Uses at least one typegpu bundled reference or explains why no extra reference was needed.",
        "Includes reduced-motion/accessibility or explains why the effect is essential and already covered.",
        "Includes cleanup/interruption/runtime validation appropriate to TypeGPU schemas, typed buffers/textures, shader functions, pipelines, WebGPU capability checks, and CPU/GPU resource ownership.",
        "Does not route the task to an unrelated near-miss skill."
      ]
    },
    {
      "id": "typegpu-task-3",
      "prompt": "Create a minimal example using tgpu that includes cleanup and accessibility notes.",
      "expected_output": "Uses typegpu routing, checks installed versions, applies domain-specific WebGPU guidance, runs or recommends the bundled audit CLI where relevant, and reports validation evidence.",
      "assertions": [
        "Mentions the relevant installed package/runtime/version checks for WebGPU.",
        "Uses at least one typegpu bundled reference or explains why no extra reference was needed.",
        "Includes reduced-motion/accessibility or explains why the effect is essential and already covered.",
        "Includes cleanup/interruption/runtime validation appropriate to TypeGPU schemas, typed buffers/textures, shader functions, pipelines, WebGPU capability checks, and CPU/GPU resource ownership.",
        "Does not route the task to an unrelated near-miss skill."
      ]
    },
    {
      "id": "typegpu-task-4",
      "prompt": "Migrate an existing motion implementation to the right WebGPU primitive without changing unrelated UI.",
      "expected_output": "Uses typegpu routing, checks installed versions, applies domain-specific WebGPU guidance, runs or recommends the bundled audit CLI where relevant, and reports validation evidence.",
      "assertions": [
        "Mentions the relevant installed package/runtime/version checks for WebGPU.",
        "Uses at least one typegpu bundled reference or explains why no extra reference was needed.",
        "Includes reduced-motion/accessibility or explains why the effect is essential and already covered.",
        "Includes cleanup/interruption/runtime validation appropriate to TypeGPU schemas, typed buffers/textures, shader functions, pipelines, WebGPU capability checks, and CPU/GPU resource ownership.",
        "Does not route the task to an unrelated near-miss skill."
      ]
    }
  ]
}
