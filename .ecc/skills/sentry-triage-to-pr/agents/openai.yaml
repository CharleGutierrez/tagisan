interface:
  display_name: "Sentry Triage To PR"
  short_description: "Rank Sentry issues and plan PR fixes"
  default_prompt: "Use $sentry-triage-to-pr to rank unresolved Sentry issues, group them into GitHub issues and PR-sized fixes, plan branches/worktrees, and babysit PR closeout with verified evidence."
policy:
  allow_implicit_invocation: false
