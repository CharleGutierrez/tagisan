{
  "evals": [
    {
      "id": "gsap-performance-task-1",
      "prompt": "In src/components/Hero.ts, implement GSAP performance behavior for a product surface and explain the validation gates.",
      "expected_output": "Uses gsap-performance routing, checks installed versions, applies domain-specific GSAP guidance, runs or recommends the bundled audit CLI where relevant, and reports validation evidence.",
      "assertions": [
        "Mentions the relevant installed package/runtime/version checks for GSAP.",
        "Uses at least one gsap-performance bundled reference or explains why no extra reference was needed.",
        "Includes reduced-motion/accessibility or explains why the effect is essential and already covered.",
        "Includes cleanup/interruption/runtime validation appropriate to GSAP performance audits: transform/opacity, layout thrash, ScrollTrigger batching, repeat loops, will-change, and frame pressure.",
        "Does not route the task to an unrelated near-miss skill."
      ]
    },
    {
      "id": "gsap-performance-task-2",
      "prompt": "Review this branch for GSAP motion issues: package versions look current but users report jank and missing reduced-motion behavior.",
      "expected_output": "Uses gsap-performance routing, checks installed versions, applies domain-specific GSAP guidance, runs or recommends the bundled audit CLI where relevant, and reports validation evidence.",
      "assertions": [
        "Mentions the relevant installed package/runtime/version checks for GSAP.",
        "Uses at least one gsap-performance bundled reference or explains why no extra reference was needed.",
        "Includes reduced-motion/accessibility or explains why the effect is essential and already covered.",
        "Includes cleanup/interruption/runtime validation appropriate to GSAP performance audits: transform/opacity, layout thrash, ScrollTrigger batching, repeat loops, will-change, and frame pressure.",
        "Does not route the task to an unrelated near-miss skill."
      ]
    },
    {
      "id": "gsap-performance-task-3",
      "prompt": "Create a minimal example using jank that includes cleanup and accessibility notes.",
      "expected_output": "Uses gsap-performance routing, checks installed versions, applies domain-specific GSAP guidance, runs or recommends the bundled audit CLI where relevant, and reports validation evidence.",
      "assertions": [
        "Mentions the relevant installed package/runtime/version checks for GSAP.",
        "Uses at least one gsap-performance bundled reference or explains why no extra reference was needed.",
        "Includes reduced-motion/accessibility or explains why the effect is essential and already covered.",
        "Includes cleanup/interruption/runtime validation appropriate to GSAP performance audits: transform/opacity, layout thrash, ScrollTrigger batching, repeat loops, will-change, and frame pressure.",
        "Does not route the task to an unrelated near-miss skill."
      ]
    },
    {
      "id": "gsap-performance-task-4",
      "prompt": "Migrate an existing motion implementation to the right GSAP primitive without changing unrelated UI.",
      "expected_output": "Uses gsap-performance routing, checks installed versions, applies domain-specific GSAP guidance, runs or recommends the bundled audit CLI where relevant, and reports validation evidence.",
      "assertions": [
        "Mentions the relevant installed package/runtime/version checks for GSAP.",
        "Uses at least one gsap-performance bundled reference or explains why no extra reference was needed.",
        "Includes reduced-motion/accessibility or explains why the effect is essential and already covered.",
        "Includes cleanup/interruption/runtime validation appropriate to GSAP performance audits: transform/opacity, layout thrash, ScrollTrigger batching, repeat loops, will-change, and frame pressure.",
        "Does not route the task to an unrelated near-miss skill."
      ]
    }
  ]
}
