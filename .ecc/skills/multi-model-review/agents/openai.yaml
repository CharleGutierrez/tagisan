interface:
  display_name: "Multi-Model Review"
  short_description: "Run parallel independent review lanes and verify findings"
  default_prompt: "Use $multi-model-review to run parallel independent review lanes and adversarially verify the merged findings."
policy:
  allow_implicit_invocation: false
