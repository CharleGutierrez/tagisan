interface:
  display_name: "Better Accessibility"
  short_description: "Focus, keyboard, ARIA, forms and screen readers"
  default_prompt: "Use $better-accessibility to build or review interactive components for focus and keyboard support, ARIA, semantic HTML, forms, hit areas, and screen-reader behavior."
policy:
  allow_implicit_invocation: true
