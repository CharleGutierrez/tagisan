# Verification

Every accepted fix must be tied to:

1. the file or subsystem it changed
2. the check command used
3. whether the check passed, failed, or was intentionally skipped

Do not mark review findings complete without verifying the relevant code path.

