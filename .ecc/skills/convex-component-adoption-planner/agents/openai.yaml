interface:
  display_name: "Convex Component Adoption Planner"
  short_description: "Plans private-app adoption or rejection of Convex components"
  default_prompt: "Use $convex-component-adoption-planner to research a Convex component for a private app and generate a `PLAN.md` plus `CODEX_FULL_PROMPT.md` package."

policy:
  allow_implicit_invocation: false
