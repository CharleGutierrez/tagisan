interface:
  display_name: "Repo Modernizer"
  short_description: "Modernize repos with dependency and framework intel"
  default_prompt: "Use $repo-modernizer to audit dependencies, research releases, plan modernization waves, and execute verified upgrades."
policy:
  allow_implicit_invocation: false
dependencies:
  tools:
    - type: "mcp"
      value: "context7"
      description: "Current library documentation"
    - type: "mcp"
      value: "exa"
      description: "Current migration and release-note research"
