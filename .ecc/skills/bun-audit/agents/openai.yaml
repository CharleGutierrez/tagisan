interface:
  display_name: "Bun Audit"
  short_description: "Shared Bun audit and safe remediation router"
  default_prompt: "Use bun-audit to run codex-dev bun audit, rules, fixes, validate, and benchmark. Keep bun-dev as the rule source of truth and avoid duplicating engine logic here."
